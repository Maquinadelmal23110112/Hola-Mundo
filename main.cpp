#include<iostream>
#include<list>
#include<Mascota.hpp>
#include<Alimento.hpp>

using namespace std;

int main(int argc, char const *argv[])
{
    Mascota m1;
    Mascota m2;

    mascotas.pushback;

    Alimento a1(5);
    Alimento a2(2);

   m1.Comer(a1);
   m2.Comer(a2);

   for (auto &&mascota : _mascotas)
   {
       std::cout << "Mascota 1, Energia:"
                 << m1.LeerEnergia()
                        std::
                 << endl;
   }

    return 0;
}
